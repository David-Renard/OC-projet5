{% extends "/backoffice/base.html.twig" %}

{% block title %}Administration du site{% endblock %}

{% block content %}
    <div class="container my-5">
        <div class="row">
            <div class="col text-center">
                {% for flash in flashes.error %}
                    <h1 class="fs-4 text-center my-5">{{ flash }}</h1>
                {% endfor %}
                {% for flash in flashes.success %}
                    <h1 class="fs-4 text-center my-5">{{ flash }}</h1>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}